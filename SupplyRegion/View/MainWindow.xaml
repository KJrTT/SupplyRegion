<Window x:Class="SupplyRegion.View.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:converters="clr-namespace:SupplyRegion.Converters"
        xmlns:vm="clr-namespace:SupplyRegion.ViewModel"
        mc:Ignorable="d"
        Title="SupplyRegion - Управление закупками" 
        Height="800" Width="1400"
        WindowStartupLocation="CenterScreen"
        Background="#F8FAFC"
        FontFamily="Segoe UI">

    <Window.DataContext>
        <vm:MainViewModel/>
    </Window.DataContext>

    <Window.Resources>
        
        <converters:StatusToColorConverter x:Key="StatusToColorConverter"/>
        <BooleanToVisibilityConverter x:Key="BoolToVis"/>

        
        <Style TargetType="Border">
            <Setter Property="CornerRadius" Value="12"/>
        </Style>

        
        <Style x:Key="MenuButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="#64748B"/>
            <Setter Property="HorizontalContentAlignment" Value="Left"/>
            <Setter Property="Padding" Value="16 12"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="Medium"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                              VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#F1F5F9"/>
                    <Setter Property="Foreground" Value="#334155"/>
                </Trigger>
                <Trigger Property="IsPressed" Value="True">
                    <Setter Property="Background" Value="#E2E8F0"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        
        <Style x:Key="StatCardStyle" TargetType="Border">
            <Setter Property="Background" Value="White"/>
            <Setter Property="BorderBrush" Value="#E2E8F0"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="20"/>
            <Setter Property="Margin" Value="0 0 16 0"/>
            <Setter Property="Effect">
                <Setter.Value>
                    <DropShadowEffect BlurRadius="25" 
                                      ShadowDepth="1" 
                                      Opacity="0.1" 
                                      Color="Black"/>
                </Setter.Value>
            </Setter>
        </Style>

        
        <Style x:Key="TitleStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="24"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Foreground" Value="#1E293B"/>
        </Style>

        
        <Style x:Key="SubtitleStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Foreground" Value="#64748B"/>
        </Style>

        
        <Style x:Key="BigNumberStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="32"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Foreground" Value="#1E293B"/>
        </Style>

        
        <Style x:Key="ActionButtonStyle" TargetType="Button">
            <Setter Property="Padding" Value="16 10"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
        </Style>

        
        <Style x:Key="ActivityItemStyle" TargetType="Border">
            <Setter Property="Background" Value="White"/>
            <Setter Property="CornerRadius" Value="8"/>
            <Setter Property="Padding" Value="16"/>
            <Setter Property="Margin" Value="0 0 0 12"/>
            <Setter Property="BorderBrush" Value="#F1F5F9"/>
            <Setter Property="BorderThickness" Value="1"/>
        </Style>

        
        <converters:StringToVisibilityConverter x:Key="StringToVisibilityConverter"/>
    </Window.Resources>

    <Grid>
        
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="280"/>
            
            <ColumnDefinition Width="*"/>
            
        </Grid.ColumnDefinitions>

        
        <Border Grid.Column="0" 
                Background="White" 
                BorderBrush="#E2E8F0" 
                BorderThickness="0 0 1 0">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    
                    <RowDefinition Height="*"/>
                    
                    <RowDefinition Height="Auto"/>
                    
                </Grid.RowDefinitions>

                <Border Grid.Row="0" 
                        Padding="24" 
                        BorderBrush="#E2E8F0" 
                        BorderThickness="0 0 0 1">
                    <StackPanel Orientation="Horizontal" 
                                HorizontalAlignment="Center">
                        <Border Width="40" 
                                Height="40" 
                                Background="#3B82F6" 
                                CornerRadius="10">
                            <TextBlock Text="📦" 
                                      FontSize="20"
                                      HorizontalAlignment="Center"
                                      VerticalAlignment="Center"/>
                        </Border>
                        <StackPanel Margin="12 0 0 0">
                            <TextBlock Text="Supply" 
                                      FontSize="20" 
                                      FontWeight="Bold" 
                                      Foreground="#1E293B"/>
                            <TextBlock Text="Region" 
                                      FontSize="20" 
                                      FontWeight="Bold" 
                                      Foreground="#3B82F6"/>
                        </StackPanel>
                    </StackPanel>
                </Border>

                <StackPanel Grid.Row="1" 
                            Margin="16 32 16 0">

                    <TextBlock Text="ДАШБОРД" 
                              FontSize="11" 
                              Foreground="#94A3B8"
                              FontWeight="Bold"
                              Margin="16 0 0 12"/>

                    <Button Content="📥 Заявки"
                           Command="{Binding ShowRequestsCommand}">
                        <Button.Style>
                            <Style TargetType="Button" BasedOn="{StaticResource MenuButtonStyle}">
                                <Setter Property="Background" Value="#F1F5F9"/>
                                <Setter Property="Foreground" Value="#3B82F6"/>
                                <Setter Property="FontWeight" Value="SemiBold"/>
                            </Style>
                        </Button.Style>
                    </Button>

                    <Button Style="{StaticResource MenuButtonStyle}"
                           Content="📊 Аналитика"
                           Command="{Binding ShowAnalyticsCommand}"/>

                    <Button Style="{StaticResource MenuButtonStyle}"
                           Content="📈 Отчеты"
                           Command="{Binding ShowReportsCommand}"/>

                    <Separator Height="1" 
                              Background="#E2E8F0" 
                              Margin="16 24 16 16"/>

                    <TextBlock Text="ФИЛЬТРЫ" 
                              FontSize="11" 
                              Foreground="#94A3B8"
                              FontWeight="Bold"
                              Margin="16 0 0 12"/>

                    <Button Style="{StaticResource MenuButtonStyle}"
                           Content="🔄 Все заявки"
                           Command="{Binding ShowAllRequestsCommand}"/>

                    <Button Style="{StaticResource MenuButtonStyle}"
                           Content="🆕 Новые"
                           Command="{Binding FilterNewCommand}"/>

                    <Button Style="{StaticResource MenuButtonStyle}"
                           Content="✅ Заказанные"
                           Command="{Binding FilterOrderedCommand}"/>

                    <Button Style="{StaticResource MenuButtonStyle}"
                           Content="📦 Полученные"
                           Command="{Binding FilterReceivedCommand}"/>

                    <Border Grid.Row="1"/>
                </StackPanel>

                <Border Grid.Row="2" Padding="8"/>
            </Grid>
        </Border>

        <ScrollViewer Grid.Column="1" 
                      VerticalScrollBarVisibility="Auto"
                      HorizontalScrollBarVisibility="Disabled">
            <Grid Margin="32">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <Grid Grid.Row="0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    

                    <StackPanel Grid.Column="1" 
                                Orientation="Horizontal">

                        <Button Content="+ Новая заявка" 
                               Command="{Binding AddRequestCommand}"
                               Style="{StaticResource ActionButtonStyle}"
                               Background="#3B82F6"
                               Foreground="White"
                               Margin="0 0 12 0">
                            <Button.Effect>
                                <DropShadowEffect BlurRadius="25" 
                                                  ShadowDepth="1" 
                                                  Opacity="0.2" 
                                                  Color="#3B82F6"/>
                            </Button.Effect>
                        </Button>

                        <Button Content="📥 Экспорт CSV"
                               Command="{Binding ExportToCsvCommand}"
                               Style="{StaticResource ActionButtonStyle}"
                               Background="White"
                               Foreground="#64748B"
                               BorderBrush="#E2E8F0"
                               BorderThickness="1">
                            <Button.Effect>
                                <DropShadowEffect BlurRadius="25" 
                                                  ShadowDepth="1" 
                                                  Opacity="0.1" 
                                                  Color="Black"/>
                            </Button.Effect>
                        </Button>

                        <Button Content="⟳"
                                ToolTip="Обновить список из БД"
                                Command="{Binding LoadDataCommand}"
                                Style="{StaticResource ActionButtonStyle}"
                                Background="White"
                                Foreground="#64748B"
                                BorderBrush="#E2E8F0"
                                BorderThickness="1"
                                Margin="8 0 0 0"/>
                    </StackPanel>
                </Grid>

                <Border Grid.Row="1" 
                       Background="White" 
                       CornerRadius="12"
                       Padding="20"
                       Margin="0 24 0 0"
                       BorderBrush="#E2E8F0"
                       BorderThickness="1">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>

                        <Border Grid.Column="0" 
                               Background="#F8FAFC" 
                               BorderBrush="#E2E8F0" 
                               BorderThickness="1"
                               Margin="0 0 12 0">
                            <Grid>
                                <TextBox Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}"
                                        BorderThickness="0"
                                        Background="Transparent"
                                        Padding="12"
                                        FontSize="14"
                                        VerticalContentAlignment="Center"
                                        Foreground="#1E293B">
                                    <TextBox.Style>
                                        <Style TargetType="TextBox">
                                            <Setter Property="Foreground" Value="#1E293B"/>
                                            <Style.Triggers>
                                                <Trigger Property="Text" Value="">
                                                    <Setter Property="Foreground" Value="#94A3B8"/>
                                                </Trigger>
                                            </Style.Triggers>
                                        </Style>
                                    </TextBox.Style>
                                </TextBox>

                                <TextBlock Text="🔍 Поиск по товару или отделу..." 
                                          IsHitTestVisible="False"
                                          VerticalAlignment="Center"
                                          HorizontalAlignment="Left"
                                          Margin="12"
                                          Foreground="#94A3B8"
                                          Visibility="{Binding SearchText, Converter={StaticResource StringToVisibilityConverter}, ConverterParameter=inverse}"/>
                            </Grid>
                        </Border>

                        <ComboBox Grid.Column="1"
                                 ItemsSource="{Binding Statuses}"
                                 SelectedItem="{Binding SelectedStatus}"
                                 Width="180"
                                 Margin="0 0 12 0"
                                 Padding="12"
                                 FontSize="14"
                                 Background="White"
                                 BorderBrush="#E2E8F0"
                                 BorderThickness="1"/>

                        <Button Grid.Column="2"
                               Content="Применить"
                               Command="{Binding FilterByStatusCommand}"
                               Style="{StaticResource ActionButtonStyle}"
                               Background="#F1F5F9"
                               Foreground="#64748B"
                               Padding="20 10"/>
                    </Grid>
                </Border>

                <WrapPanel Grid.Row="2" 
                          Margin="0 24 0 0"
                          HorizontalAlignment="Stretch">

                    <Border Style="{StaticResource StatCardStyle}" 
                           Width="240" 
                           Height="140">
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="*"/>
                            </Grid.RowDefinitions>

                            <StackPanel Grid.Row="0" 
                                       Orientation="Horizontal">
                                <Border Width="40" 
                                       Height="40" 
                                       Background="#DBEAFE" 
                                       CornerRadius="10">
                                    <TextBlock Text="📋" 
                                              FontSize="18"
                                              HorizontalAlignment="Center"
                                              VerticalAlignment="Center"/>
                                </Border>
                                <TextBlock Text="Всего заявок" 
                                          Margin="12 0 0 0"
                                          VerticalAlignment="Center"
                                          Foreground="#64748B"
                                          FontSize="14"/>
                            </StackPanel>

                            <StackPanel Grid.Row="1" 
                                       VerticalAlignment="Bottom">
                                <TextBlock Text="{Binding TotalRequests}" 
                                          Style="{StaticResource BigNumberStyle}"/>
                                <TextBlock Text="{Binding NewRequestsCount, StringFormat='{}🆕 {0} новых'}" 
                                          Foreground="#3B82F6"
                                          FontSize="12"
                                          FontWeight="SemiBold"
                                          Margin="0 8 0 0"/>
                            </StackPanel>
                        </Grid>
                    </Border>

                    <Border Style="{StaticResource StatCardStyle}" 
                           Width="240" 
                           Height="140">
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="*"/>
                            </Grid.RowDefinitions>

                            <StackPanel Grid.Row="0" 
                                       Orientation="Horizontal">
                                <Border Width="40" 
                                       Height="40" 
                                       Background="#DCFCE7" 
                                       CornerRadius="10">
                                    <TextBlock Text="💰" 
                                              FontSize="18"
                                              HorizontalAlignment="Center"
                                              VerticalAlignment="Center"/>
                                </Border>
                                <TextBlock Text="Общая сумма" 
                                          Margin="12 0 0 0"
                                          VerticalAlignment="Center"
                                          Foreground="#64748B"
                                          FontSize="14"/>
                            </StackPanel>

                            <StackPanel Grid.Row="1" 
                                       VerticalAlignment="Bottom">
                                <TextBlock Text="{Binding TotalAmount, StringFormat='{}₽{0:N0}'}" 
                                          Style="{StaticResource BigNumberStyle}"/>
                                <TextBlock Text="{Binding OrderedAmount, StringFormat='{}✅ ₽{0:N0} заказано'}" 
                                          Foreground="#10B981"
                                          FontSize="12"
                                          FontWeight="SemiBold"
                                          Margin="0 8 0 0"/>
                            </StackPanel>
                        </Grid>
                    </Border>

                    <Border Style="{StaticResource StatCardStyle}" 
                           Width="240" 
                           Height="140">
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="*"/>
                            </Grid.RowDefinitions>

                            <StackPanel Grid.Row="0" 
                                       Orientation="Horizontal">
                                <Border Width="40" 
                                       Height="40" 
                                       Background="#FEF3C7" 
                                       CornerRadius="10">
                                    <TextBlock Text="📊" 
                                              FontSize="18"
                                              HorizontalAlignment="Center"
                                              VerticalAlignment="Center"/>
                                </Border>
                                <TextBlock Text="Средняя цена" 
                                          Margin="12 0 0 0"
                                          VerticalAlignment="Center"
                                          Foreground="#64748B"
                                          FontSize="14"/>
                            </StackPanel>

                            <StackPanel Grid.Row="1" 
                                       VerticalAlignment="Bottom">
                                <TextBlock Text="{Binding AveragePrice, StringFormat='{}₽{0:N0}'}" 
                                          Style="{StaticResource BigNumberStyle}"/>
                                <TextBlock Text="{Binding AverageQuantity, StringFormat='{}📦 {0} шт/заявка'}" 
                                          Foreground="#F59E0B"
                                          FontSize="12"
                                          FontWeight="SemiBold"
                                          Margin="0 8 0 0"/>
                            </StackPanel>
                        </Grid>
                    </Border>

                    <Border Style="{StaticResource StatCardStyle}" 
                           Width="240" 
                           Height="140">
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="*"/>
                            </Grid.RowDefinitions>

                            <StackPanel Grid.Row="0" 
                                       Orientation="Horizontal">
                                <Border Width="40" 
                                       Height="40" 
                                       Background="#E0E7FF" 
                                       CornerRadius="10">
                                    <TextBlock Text="📈" 
                                              FontSize="18"
                                              HorizontalAlignment="Center"
                                              VerticalAlignment="Center"/>
                                </Border>
                                <TextBlock Text="Завершено" 
                                          Margin="12 0 0 0"
                                          VerticalAlignment="Center"
                                          Foreground="#64748B"
                                          FontSize="14"/>
                            </StackPanel>

                            <StackPanel Grid.Row="1" 
                                       VerticalAlignment="Bottom">
                                <TextBlock Text="{Binding CompletionRate, StringFormat='{}{0}%'}" 
                                          Style="{StaticResource BigNumberStyle}"/>

                                <Border Height="6" 
                                       Background="#E2E8F0" 
                                       CornerRadius="3"
                                       Margin="0 12 0 0">
                                    <Border Width="{Binding CompletionRatePercent}" 
                                           Background="#3B82F6" 
                                           CornerRadius="3"
                                           HorizontalAlignment="Left"/>
                                </Border>
                            </StackPanel>
                        </Grid>
                    </Border>
                </WrapPanel>

                <Grid Grid.Row="3" 
                      Margin="0 32 0 0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="400"/>
                    </Grid.ColumnDefinitions>

                    <Border Grid.Column="0" 
                           Margin="0 0 16 0"
                           Background="White" 
                           CornerRadius="12"
                           BorderBrush="#E2E8F0"
                           BorderThickness="1">
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="*"/>
                            </Grid.RowDefinitions>

                            <Border Grid.Row="0" 
                                   Background="#F8FAFC" 
                                   Padding="24"
                                   BorderBrush="#E2E8F0"
                                   BorderThickness="0 0 0 1">
                                <StackPanel Orientation="Horizontal" 
                                           HorizontalAlignment="Stretch">
                                    <TextBlock Text="Все заявки" 
                                              FontSize="18" 
                                              FontWeight="Bold"
                                              Foreground="#1E293B"/>

                                    <TextBlock Text="{Binding Requests.Count, StringFormat='{}Всего: {0}'}" 
                                              FontSize="14"
                                              Foreground="#64748B"/>
                                </StackPanel>
                            </Border>

                            <DataGrid Grid.Row="1"
                                     ItemsSource="{Binding Requests}"
                                     SelectedItem="{Binding SelectedRequest}"
                                     AutoGenerateColumns="False"
                                     CanUserAddRows="False"
                                     IsReadOnly="True"
                                     GridLinesVisibility="None"
                                     RowBackground="Transparent"
                                     AlternatingRowBackground="#F8FAFC"
                                     BorderThickness="0"
                                     HeadersVisibility="Column"
                                     SelectionUnit="FullRow"
                                     VerticalScrollBarVisibility="Auto"
                                     HorizontalScrollBarVisibility="Auto">

                                <DataGrid.Resources>
                                    <Style TargetType="DataGridColumnHeader">
                                        <Setter Property="Background" Value="#F8FAFC"/>
                                        <Setter Property="BorderBrush" Value="#E2E8F0"/>
                                        <Setter Property="BorderThickness" Value="0 0 0 1"/>
                                        <Setter Property="Foreground" Value="#64748B"/>
                                        <Setter Property="FontWeight" Value="SemiBold"/>
                                        <Setter Property="Padding" Value="16"/>
                                        <Setter Property="HorizontalContentAlignment" Value="Left"/>
                                        <Setter Property="FontSize" Value="13"/>
                                    </Style>

                                    <Style TargetType="DataGridRow">
                                        <Setter Property="BorderBrush" Value="Transparent"/>
                                        <Setter Property="BorderThickness" Value="0"/>
                                        <Setter Property="MinHeight" Value="56"/>
                                        <Style.Triggers>
                                            <Trigger Property="IsMouseOver" Value="True">
                                                <Setter Property="Background" Value="#F8FAFC"/>
                                            </Trigger>
                                            <Trigger Property="IsSelected" Value="True">
                                                <Setter Property="Background" Value="#EFF6FF"/>
                                            </Trigger>
                                        </Style.Triggers>
                                    </Style>


                                    <Style TargetType="DataGridCell">
                                        <Setter Property="BorderBrush" Value="#E2E8F0"/>
                                        <Setter Property="BorderThickness" Value="0 0 0 1"/>
                                        <Setter Property="Foreground" Value="#1E293B"/>
                                        <Setter Property="Padding" Value="16"/>
                                        <Setter Property="VerticalAlignment" Value="Center"/>
                                        <Style.Triggers>
                                            <Trigger Property="IsSelected" Value="True">
                                                <Setter Property="Background" Value="Transparent"/>
                                                <Setter Property="Foreground" Value="#1E293B"/>
                                                <Setter Property="BorderBrush" Value="#3B82F6"/>
                                            </Trigger>
                                        </Style.Triggers>
                                    </Style>
                                </DataGrid.Resources>

                                <DataGrid.Columns>
                                    <DataGridTextColumn Header="№" 
                                                       Binding="{Binding Id}" 
                                                       Width="60"/>

                                    <DataGridTextColumn Header="ТОВАР" 
                                                       Binding="{Binding ProductName}" 
                                                       Width="200">
                                        <DataGridTextColumn.ElementStyle>
                                            <Style TargetType="TextBlock">
                                                <Setter Property="FontWeight" Value="SemiBold"/>
                                                <Setter Property="TextTrimming" Value="CharacterEllipsis"/>
                                            </Style>
                                        </DataGridTextColumn.ElementStyle>
                                    </DataGridTextColumn>

                                    <DataGridTextColumn Header="ОТДЕЛ" 
                                                       Binding="{Binding Department}" 
                                                       Width="120"/>

                                    <DataGridTextColumn Header="КОЛ-ВО" 
                                                       Binding="{Binding Quantity}" 
                                                       Width="80"/>

                                    <DataGridTextColumn Header="ЦЕНА" 
                                                       Binding="{Binding EstimatedPrice, StringFormat='{}₽{0:N0}'}" 
                                                       Width="100">
                                        <DataGridTextColumn.ElementStyle>
                                            <Style TargetType="TextBlock">
                                                <Setter Property="Foreground" Value="#10B981"/>
                                                <Setter Property="FontWeight" Value="SemiBold"/>
                                            </Style>
                                        </DataGridTextColumn.ElementStyle>
                                    </DataGridTextColumn>

                                    <DataGridTemplateColumn Header="СТАТУС" Width="130">
                                        <DataGridTemplateColumn.CellTemplate>
                                            <DataTemplate>
                                                <Border Background="{Binding Status, Converter={StaticResource StatusToColorConverter}}"
                                                       CornerRadius="16"
                                                       Padding="8 4"
                                                       HorizontalAlignment="Left">
                                                    <TextBlock Text="{Binding Status}" 
                                                              FontSize="11"
                                                              FontWeight="SemiBold"
                                                              HorizontalAlignment="Center"/>
                                                </Border>
                                            </DataTemplate>
                                        </DataGridTemplateColumn.CellTemplate>
                                    </DataGridTemplateColumn>

                                    <DataGridTextColumn Header="ДАТА" 
                                                       Binding="{Binding CreatedDate, StringFormat=dd.MM.yyyy}" 
                                                       Width="100"/>

                                    <DataGridTemplateColumn Header="" Width="60">
                                        <DataGridTemplateColumn.CellTemplate>
                                            <DataTemplate>
                                                <Button Content="⋮" 
                                                       Background="Transparent"
                                                       BorderThickness="0"
                                                       Padding="8"
                                                       FontSize="16"
                                                       Foreground="#94A3B8"
                                                       Cursor="Hand"
                                                       Click="ActionsButton_Click">
                                                    <Button.ContextMenu>
                                                        <ContextMenu>
                                                            <MenuItem Header="✏️ Изменить статус" 
                                                                     Command="{Binding EditStatusCommand}"
                                                                     CommandParameter="{Binding PlacementTarget.DataContext, RelativeSource={RelativeSource AncestorType=ContextMenu}}"/>
                                                            <MenuItem Header="❌ Удалить" 
                                                                     Command="{Binding DeleteRequestCommand}"
                                                                     CommandParameter="{Binding PlacementTarget.DataContext, RelativeSource={RelativeSource AncestorType=ContextMenu}}"/>
                                                            <Separator/>
                                                            <MenuItem Header="📥 Экспорт в CSV"
                                                                     Command="{Binding ExportSingleToCsvCommand}"
                                                                     CommandParameter="{Binding PlacementTarget.DataContext, RelativeSource={RelativeSource AncestorType=ContextMenu}}"/>
                                                        </ContextMenu>
                                                    </Button.ContextMenu>
                                                </Button>
                                            </DataTemplate>
                                        </DataGridTemplateColumn.CellTemplate>
                                    </DataGridTemplateColumn>
                                </DataGrid.Columns>
                            </DataGrid>
                        </Grid>
                    </Border>

                    <Grid Grid.Column="1">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="*"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>

                        <Border Grid.Row="0"
                               Background="White" 
                               CornerRadius="12"
                               Padding="24"
                               Margin="0 0 0 16"
                               BorderBrush="#E2E8F0"
                               BorderThickness="1">
                            <StackPanel>
                                <TextBlock Text="Последние действия" 
                                          FontSize="18" 
                                          FontWeight="Bold"
                                          Foreground="#1E293B"
                                          Margin="0 0 0 16"/>

                                <ItemsControl ItemsSource="{Binding RecentActivities}">
                                    <ItemsControl.ItemTemplate>
                                        <DataTemplate>
                                            <Border Style="{StaticResource ActivityItemStyle}">
                                                <Grid>
                                                    <Grid.ColumnDefinitions>
                                                        <ColumnDefinition Width="Auto"/>
                                                        <ColumnDefinition Width="*"/>
                                                    </Grid.ColumnDefinitions>

                                                    <Border Grid.Column="0" 
                                                           Width="36" 
                                                           Height="36" 
                                                           Background="#F1F5F9" 
                                                           CornerRadius="18">
                                                        <TextBlock Text="{Binding Icon}" 
                                                                  FontSize="14"
                                                                  HorizontalAlignment="Center"
                                                                  VerticalAlignment="Center"/>
                                                    </Border>

                                                    <StackPanel Grid.Column="1" 
                                                               Margin="12 0 0 0">
                                                        <TextBlock Text="{Binding Message}" 
                                                                  FontSize="13"
                                                                  Foreground="#1E293B"
                                                                  TextWrapping="Wrap"/>
                                                        <TextBlock Text="{Binding Timestamp}" 
                                                                  FontSize="11"
                                                                  Foreground="#94A3B8"
                                                                  Margin="0 4 0 0"/>
                                                    </StackPanel>
                                                </Grid>
                                            </Border>
                                        </DataTemplate>
                                    </ItemsControl.ItemTemplate>
                                </ItemsControl>
                            </StackPanel>
                        </Border>

                        <Border Grid.Row="1"
                               Background="#F0F9FF"
                               CornerRadius="12"
                               Padding="24"
                               BorderBrush="#BAE6FD"
                               BorderThickness="1">
                            <StackPanel>

                                <StackPanel Orientation="Horizontal">
                                    <Border Width="36" 
                                           Height="36" 
                                           Background="#38BDF8" 
                                           CornerRadius="18">
                                        <TextBlock Text="📈" 
                                                  FontSize="14"
                                                  HorizontalAlignment="Center"
                                                  VerticalAlignment="Center"/>
                                    </Border>
                                    <StackPanel Margin="12 0 0 0">
                                        <TextBlock Text="{Binding ActiveRequests}" 
                                                  FontSize="20"
                                                  FontWeight="Bold"
                                                  Foreground="#0C4A6E"/>
                                        <TextBlock Text="Активных заявок" 
                                                  FontSize="12"
                                                  Foreground="#0C4A6E"
                                                  Opacity="0.8"/>
                                    </StackPanel>
                                </StackPanel>


                                <TextBlock Text="{Binding CompletionRate, StringFormat='{}{0}% завершено'}" 
                                          FontSize="14"
                                          FontWeight="SemiBold"
                                          Foreground="#0C4A6E"
                                          Margin="0 16 0 0"/>

                                <Border Height="8" 
                                       Background="#BAE6FD" 
                                       CornerRadius="4"
                                       Margin="0 8 0 0">
                                    <Border Width="{Binding CompletionRatePercent}" 
                                           Background="#0EA5E9" 
                                           CornerRadius="4"
                                           HorizontalAlignment="Left"/>
                                </Border>

                                <TextBlock Text="Статистика за текущий месяц" 
                                          FontSize="12"
                                          Foreground="#0C4A6E"
                                          Opacity="0.8"
                                          Margin="0 12 0 0"/>
                            </StackPanel>
                        </Border>
                    </Grid>
                </Grid>
            </Grid>
        </ScrollViewer>

        <Border Grid.Column="0" Grid.ColumnSpan="2" 
               Background="#80000000" 
               Visibility="{Binding IsBusy, Converter={StaticResource BoolToVis}}">
            <StackPanel VerticalAlignment="Center" 
                       HorizontalAlignment="Center">
                <Border Width="80" 
                       Height="80" 
                       Background="White" 
                       CornerRadius="12"
                       Padding="20">
                    <ProgressBar IsIndeterminate="True" 
                                VerticalAlignment="Center"/>
                </Border>
                <TextBlock Text="Загрузка..." 
                          Foreground="White" 
                          FontSize="14"
                          FontWeight="SemiBold"
                          Margin="0 16 0 0"/>
            </StackPanel>
        </Border>
    </Grid>
</Window>
